var n=prompt("Enter number for fibonacci")
fibonacciGenerator(n);
function fibonacciGenerator(n)
{
    if(n==1)
    return([0]);
    if(n==2)
    return([0,1]);
    var fib=[0,1];
    for(var i=2;i<n;i++)
    fib[i]=fib[i-1]+fib[i-2];
    return fib;
}
